import styled  from "styled-components"

function Header() {
    return (
        <HeaderContainer>
            HEloo
        </HeaderContainer>
    )
}

export default Header

const HeaderContainer = styled.div`
    color: "red";
`